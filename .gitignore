# Engine build outputs — regenerated deterministically from the corpus by `./rag rebuild`.
data/rag/
__pycache__/
*.pyc
# Local vector store + downloaded ONNX model (optional --embed layer)
chroma_db/
model/
wiki/corpus-orienteer/
# dream-fence runtime artifacts (schema log + operator escape hatch)
.claude/hooks/fence_log.jsonl
.claude/hooks/fence_override
