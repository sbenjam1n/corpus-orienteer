# RAG setup → see README.md

The complete operational reference (install, the build pipeline, every runnable script,
the full `query.py` CLI with all modes + flags, the concept glossary, data files, the
`domain_config.json` porting schema, and the maintenance/reconciliation loop) is in:

**[`README.md`](README.md)**

*(This file was the original setup doc; it was consolidated into README.md on 2026-06-16 to
keep a single source of truth. The filename is retained so existing references resolve.)*
